openapi: 3.1.0
info:
  # Do not change the title, if the title changes, the import paths will be broken
  title: Api
  version: 0.1.0
  description: Model Orchestrator & Source Harvester API
servers:
  - url: /api
    description: Base API path
tags:
  - name: health
    description: Health operations
  - name: models
    description: Model registry operations
  - name: runs
    description: Harvest run operations
  - name: harvest
    description: Harvest pipeline control
  - name: router
    description: Smart model selection
  - name: stats
    description: Dashboard statistics
  - name: trader-account
    description: Paper trading account state
  - name: trader-settings
    description: Trader configuration and risk profile
  - name: trader-snapshot
    description: Live market snapshot from data source
  - name: trader-signals
    description: Trading signal generation, audit, approval
  - name: trader-positions
    description: Open and closed paper positions
  - name: trader-cycle
    description: Trader decision cycle control
paths:
  /healthz:
    get:
      operationId: healthCheck
      tags: [health]
      summary: Health check
      description: Returns server health status
      responses:
        "200":
          description: Healthy
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthStatus"

  /models:
    get:
      operationId: listModels
      tags: [models]
      summary: List models in the registry
      parameters:
        - name: status
          in: query
          required: false
          schema:
            type: string
            enum: [ACTIVE, SLOW, FAIL, ARCHIVED, ALL]
            default: ALL
        - name: type
          in: query
          required: false
          schema:
            type: string
            enum: [chat, embedding, completion, image, audio, unknown, ALL]
            default: ALL
        - name: sort
          in: query
          required: false
          schema:
            type: string
            enum: [score, latency, tps, lastChecked]
            default: score
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 200
            default: 50
        - name: offset
          in: query
          required: false
          schema:
            type: integer
            minimum: 0
            default: 0
      responses:
        "200":
          description: Models list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ModelListResponse"

  /models/top:
    get:
      operationId: getTopModels
      tags: [models]
      summary: Top K models by score
      parameters:
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 20
        - name: type
          in: query
          required: false
          schema:
            type: string
            enum: [chat, embedding, completion, image, audio, unknown, ALL]
            default: ALL
      responses:
        "200":
          description: Top models
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Model"

  /models/active:
    get:
      operationId: getActiveModels
      tags: [models]
      summary: All currently active models
      responses:
        "200":
          description: Active models
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Model"

  /models/{id}:
    get:
      operationId: getModel
      tags: [models]
      summary: Model details with performance history
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Model details
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ModelDetail"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"

  /runs:
    get:
      operationId: listRuns
      tags: [runs]
      summary: Harvest run history
      parameters:
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 20
      responses:
        "200":
          description: Runs list
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/HarvestRun"

  /runs/latest:
    get:
      operationId: getLatestRun
      tags: [runs]
      summary: Latest harvest run summary
      responses:
        "200":
          description: Latest run
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HarvestRun"

  /harvest/run:
    post:
      operationId: triggerHarvest
      tags: [harvest]
      summary: Manually trigger a harvest cycle
      responses:
        "202":
          description: Accepted, run started
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HarvestStartResponse"
        "409":
          description: A run is already in progress
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"

  /harvest/status:
    get:
      operationId: getHarvestStatus
      tags: [harvest]
      summary: Current harvest pipeline status
      responses:
        "200":
          description: Status
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HarvestStatus"

  /route:
    post:
      operationId: routeRequest
      tags: [router]
      summary: Select the best active model for a task
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RouteRequest"
      responses:
        "200":
          description: Selected model
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RouteResponse"
        "404":
          description: No active model available
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"

  /stats:
    get:
      operationId: getStats
      tags: [stats]
      summary: Dashboard summary statistics
      responses:
        "200":
          description: Stats
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Stats"


  /trader/account:
    get:
      operationId: getTraderAccount
      tags: [trader-account]
      summary: Paper trading account state
      responses:
        "200":
          description: Account
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TraderAccount"

  /trader/account/reset:
    post:
      operationId: resetTraderAccount
      tags: [trader-account]
      summary: Reset paper account to a starting balance
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ResetAccountBody"
      responses:
        "200":
          description: Reset
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TraderAccount"

  /trader/settings:
    get:
      operationId: getTraderSettings
      tags: [trader-settings]
      summary: Trader settings (risk profile, mode, execution)
      responses:
        "200":
          description: Settings
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TraderSettings"
    patch:
      operationId: updateTraderSettings
      tags: [trader-settings]
      summary: Update trader settings
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateTraderSettingsRequest"
      responses:
        "200":
          description: Updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TraderSettings"

  /trader/snapshot:
    get:
      operationId: getTraderSnapshot
      tags: [trader-snapshot]
      summary: Current XAU/USD market snapshot from source
      responses:
        "200":
          description: Snapshot
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TraderSnapshot"
        "503":
          description: Data source unreachable
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"

  /trader/signals:
    get:
      operationId: listTraderSignals
      tags: [trader-signals]
      summary: List signals (most recent first)
      parameters:
        - name: status
          in: query
          required: false
          schema:
            type: string
            enum: [PENDING, APPROVED, REJECTED, EXECUTED, EXPIRED, ALL]
            default: ALL
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 200
            default: 50
      responses:
        "200":
          description: Signals
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/TraderSignal"

  /trader/signals/{id}:
    get:
      operationId: getTraderSignal
      tags: [trader-signals]
      summary: Signal with full audit trail
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Signal
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TraderSignalDetail"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"

  /trader/signals/{id}/approve:
    post:
      operationId: approveTraderSignal
      tags: [trader-signals]
      summary: Approve a pending signal (manual mode)
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Approved and executed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TraderSignalDetail"
        "409":
          description: Cannot approve in current state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"

  /trader/signals/{id}/reject:
    post:
      operationId: rejectTraderSignal
      tags: [trader-signals]
      summary: Reject a pending signal
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RejectSignalRequest"
      responses:
        "200":
          description: Rejected
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TraderSignal"

  /trader/positions:
    get:
      operationId: listTraderPositions
      tags: [trader-positions]
      summary: Open and closed paper positions
      parameters:
        - name: status
          in: query
          required: false
          schema:
            type: string
            enum: [OPEN, CLOSED, ALL]
            default: ALL
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 200
            default: 50
      responses:
        "200":
          description: Positions
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/TraderPosition"

  /trader/positions/{id}/close:
    post:
      operationId: closeTraderPosition
      tags: [trader-positions]
      summary: Manually close an open position at current market price
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Closed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TraderPosition"
        "409":
          description: Position not open
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"

  /trader/cycle/run:
    post:
      operationId: runTraderCycle
      tags: [trader-cycle]
      summary: Manually trigger one decision cycle now
      responses:
        "200":
          description: Cycle ran
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CycleRunResult"

  /trader/decision:
    get:
      operationId: getTraderDecision
      tags: [trader-cycle]
      summary: Run multi-agent consensus engine (dry-run, no trade committed)
      responses:
        "200":
          description: Consensus verdict from all 6 agents + anti-hallucination guard
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConsensusVerdict"
        "503":
          description: Decision engine failed (source unreachable)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"

  /trader/ingest/frame:
    post:
      operationId: ingestTraderFrame
      tags: [trader-cycle]
      summary: Ingest a Bookmap/heatmap frame for the Vision Agent
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/FrameIngestRequest"
      responses:
        "200":
          description: Frame ingested
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/IngestFrameResult"

  /trader/dashboard:
    get:
      operationId: getTraderDashboard
      tags: [trader-account]
      summary: Aggregated dashboard payload (account + open positions + recent signals + last cycle)
      responses:
        "200":
          description: Dashboard
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TraderDashboard"

components:
  schemas:
    HealthStatus:
      type: object
      properties:
        status:
          type: string
      required:
        - status

    Error:
      type: object
      properties:
        error:
          type: string
        message:
          type: string
      required:
        - error
        - message

    Model:
      type: object
      properties:
        id:
          type: string
        name:
          type: string
        provider:
          type: string
        endpoint:
          type: string
        type:
          type: string
          enum: [chat, embedding, completion, image, audio, unknown]
        latencyMs:
          type: number
          nullable: true
        tps:
          type: number
          nullable: true
        successRate:
          type: number
          nullable: true
        quality:
          type: number
          nullable: true
        score:
          type: number
        status:
          type: string
          enum: [ACTIVE, SLOW, FAIL, ARCHIVED]
        rank:
          type: integer
          nullable: true
        lastChecked:
          type: string
          format: date-time
          nullable: true
        sourceUrl:
          type: string
          nullable: true
        notes:
          type: string
          nullable: true
      required:
        - id
        - name
        - provider
        - endpoint
        - type
        - score
        - status

    ModelListResponse:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/Model"
        total:
          type: integer
        limit:
          type: integer
        offset:
          type: integer
      required:
        - items
        - total
        - limit
        - offset

    ModelHistoryPoint:
      type: object
      properties:
        runId:
          type: string
        timestamp:
          type: string
          format: date-time
        latencyMs:
          type: number
          nullable: true
        tps:
          type: number
          nullable: true
        score:
          type: number
        status:
          type: string
      required:
        - runId
        - timestamp
        - score
        - status

    ModelDetail:
      allOf:
        - $ref: "#/components/schemas/Model"
        - type: object
          properties:
            history:
              type: array
              items:
                $ref: "#/components/schemas/ModelHistoryPoint"
          required:
            - history

    HarvestRun:
      type: object
      properties:
        id:
          type: string
        startedAt:
          type: string
          format: date-time
        finishedAt:
          type: string
          format: date-time
          nullable: true
        status:
          type: string
          enum: [RUNNING, SUCCESS, FAILED]
        sourceCommit:
          type: string
          nullable: true
        linksDiscovered:
          type: integer
        linksTested:
          type: integer
        modelsActive:
          type: integer
        modelsFailed:
          type: integer
        avgLatencyMs:
          type: number
          nullable: true
        avgScore:
          type: number
          nullable: true
        durationMs:
          type: integer
          nullable: true
        message:
          type: string
          nullable: true
      required:
        - id
        - startedAt
        - status
        - linksDiscovered
        - linksTested
        - modelsActive
        - modelsFailed

    HarvestStartResponse:
      type: object
      properties:
        runId:
          type: string
        startedAt:
          type: string
          format: date-time
        message:
          type: string
      required:
        - runId
        - startedAt
        - message

    HarvestStatus:
      type: object
      properties:
        running:
          type: boolean
        currentRunId:
          type: string
          nullable: true
        stage:
          type: string
          nullable: true
        progress:
          type: number
          nullable: true
        startedAt:
          type: string
          format: date-time
          nullable: true
        lastFinishedAt:
          type: string
          format: date-time
          nullable: true
        nextScheduledAt:
          type: string
          format: date-time
          nullable: true
      required:
        - running

    RouteRequest:
      type: object
      properties:
        taskType:
          type: string
          enum: [realtime, analysis, balanced, embedding]
        modelType:
          type: string
          enum: [chat, embedding, completion, image, audio, ALL]
          default: chat
        excludeIds:
          type: array
          items:
            type: string
      required:
        - taskType

    RouteResponse:
      type: object
      properties:
        taskType:
          type: string
        selected:
          $ref: "#/components/schemas/Model"
        fallbacks:
          type: array
          items:
            $ref: "#/components/schemas/Model"
        reason:
          type: string
      required:
        - taskType
        - selected
        - fallbacks
        - reason

    Stats:
      type: object
      properties:
        totalModels:
          type: integer
        activeModels:
          type: integer
        slowModels:
          type: integer
        failedModels:
          type: integer
        archivedModels:
          type: integer
        avgScore:
          type: number
          nullable: true
        avgLatencyMs:
          type: number
          nullable: true
        avgTps:
          type: number
          nullable: true
        lastRunAt:
          type: string
          format: date-time
          nullable: true
        lastRunStatus:
          type: string
          nullable: true
        nextRunAt:
          type: string
          format: date-time
          nullable: true
        typeBreakdown:
          type: array
          items:
            type: object
            properties:
              type:
                type: string
              count:
                type: integer
            required:
              - type
              - count
      required:
        - totalModels
        - activeModels
        - slowModels
        - failedModels
        - archivedModels
        - typeBreakdown
    ErrorBody:
      type: object
      properties:
        error:
          type: string
      required: [error]

    ResetAccountBody:
      type: object
      properties:
        startingBalance:
          type: number
          minimum: 1
          default: 100000
      required: []

    TraderAccount:
      type: object
      properties:
        balance:
          type: number
        equity:
          type: number
        startingBalance:
          type: number
        realizedPnl:
          type: number
        unrealizedPnl:
          type: number
        peakEquity:
          type: number
        currentDrawdownPct:
          type: number
        dailyPnl:
          type: number
        dailyPnlResetAt:
          type: string
          format: date-time
        openPositions:
          type: integer
        tradesToday:
          type: integer
        totalTrades:
          type: integer
        winRate:
          type: number
          nullable: true
        updatedAt:
          type: string
          format: date-time
      required:
        - balance
        - equity
        - startingBalance
        - realizedPnl
        - unrealizedPnl
        - peakEquity
        - currentDrawdownPct
        - dailyPnl
        - dailyPnlResetAt
        - openPositions
        - tradesToday
        - totalTrades
        - updatedAt

    TraderSettings:
      type: object
      properties:
        executionMode:
          type: string
          enum: [OFF, MANUAL, AUTO]
        tradingMode:
          type: string
          enum: [DAILY, MID]
        riskPerTradePct:
          type: number
          minimum: 0.05
          maximum: 5
        dailyLossCapPct:
          type: number
          minimum: 0.5
          maximum: 20
        maxOpenPositions:
          type: integer
          minimum: 1
          maximum: 10
        maxTradesPerDay:
          type: integer
          minimum: 1
          maximum: 50
        minConfidence:
          type: number
          minimum: 0
          maximum: 1
        minRiskReward:
          type: number
          minimum: 0.5
          maximum: 5
        requireAiConfirmation:
          type: boolean
        aiConfirmCount:
          type: integer
          minimum: 1
          maximum: 5
        signalExpirySec:
          type: integer
          minimum: 30
          maximum: 3600
        updatedAt:
          type: string
          format: date-time
      required:
        - executionMode
        - tradingMode
        - riskPerTradePct
        - dailyLossCapPct
        - maxOpenPositions
        - maxTradesPerDay
        - minConfidence
        - minRiskReward
        - requireAiConfirmation
        - aiConfirmCount
        - signalExpirySec
        - updatedAt

    UpdateTraderSettingsRequest:
      type: object
      properties:
        executionMode:
          type: string
          enum: [OFF, MANUAL, AUTO]
        tradingMode:
          type: string
          enum: [DAILY, MID]
        riskPerTradePct:
          type: number
        dailyLossCapPct:
          type: number
        maxOpenPositions:
          type: integer
        maxTradesPerDay:
          type: integer
        minConfidence:
          type: number
        minRiskReward:
          type: number
        requireAiConfirmation:
          type: boolean
        aiConfirmCount:
          type: integer
        signalExpirySec:
          type: integer

    RejectSignalRequest:
      type: object
      properties:
        reason:
          type: string

    TraderSnapshot:
      type: object
      properties:
        fetchedAt:
          type: string
          format: date-time
        sourceStatus:
          type: string
        symbol:
          type: string
        spot:
          type: number
        atrPct:
          type: number
          nullable: true
        atrAbs:
          type: number
          nullable: true
        signalDirection:
          type: string
          enum: [BUY, SELL, NEUTRAL]
        signalConfidence:
          type: number
        signalScore:
          type: number
        signalEntry:
          type: number
          nullable: true
        signalStopLoss:
          type: number
          nullable: true
        signalTakeProfits:
          type: array
          items:
            type: number
        signalRiskReward:
          type: number
          nullable: true
        timingState:
          type: string
          nullable: true
        timingPressure:
          type: number
          nullable: true
        macroSummary:
          type: object
          additionalProperties: true
        cotTilt:
          type: string
          nullable: true
        newsHighImpactCount:
          type: integer
        drivers:
          type: array
          items:
            type: string
      required:
        - fetchedAt
        - sourceStatus
        - symbol
        - spot
        - signalDirection
        - signalConfidence
        - signalScore
        - signalTakeProfits
        - newsHighImpactCount
        - drivers

    TraderSignal:
      type: object
      properties:
        id:
          type: string
        createdAt:
          type: string
          format: date-time
        tradingMode:
          type: string
        executionMode:
          type: string
        direction:
          type: string
          enum: [BUY, SELL]
        confidence:
          type: number
        sourceScore:
          type: number
        entry:
          type: number
        stopLoss:
          type: number
        takeProfit:
          type: number
        riskReward:
          type: number
        atrAbs:
          type: number
        sizeUnits:
          type: number
        riskAmount:
          type: number
        rulesPassed:
          type: boolean
        aiPassed:
          type: boolean
          nullable: true
        aiVotersCount:
          type: integer
        aiAgreeCount:
          type: integer
        status:
          type: string
          enum: [PENDING, APPROVED, REJECTED, EXECUTED, EXPIRED]
        rejectionReason:
          type: string
          nullable: true
        positionId:
          type: string
          nullable: true
      required:
        - id
        - createdAt
        - tradingMode
        - executionMode
        - direction
        - confidence
        - sourceScore
        - entry
        - stopLoss
        - takeProfit
        - riskReward
        - atrAbs
        - sizeUnits
        - riskAmount
        - rulesPassed
        - aiVotersCount
        - aiAgreeCount
        - status

    GateDecision:
      type: object
      properties:
        gate:
          type: string
        passed:
          type: boolean
        reason:
          type: string
        value:
          type: number
          nullable: true
        threshold:
          type: number
          nullable: true
      required: [gate, passed, reason]

    AiVote:
      type: object
      properties:
        modelId:
          type: string
        modelName:
          type: string
        direction:
          type: string
          enum: [BUY, SELL, NEUTRAL, ABSTAIN]
        rationale:
          type: string
          nullable: true
        latencyMs:
          type: number
          nullable: true
        agreed:
          type: boolean
      required: [modelId, modelName, direction, agreed]

    TraderSignalDetail:
      allOf:
        - $ref: "#/components/schemas/TraderSignal"
        - type: object
          properties:
            snapshot:
              $ref: "#/components/schemas/TraderSnapshot"
            gates:
              type: array
              items:
                $ref: "#/components/schemas/GateDecision"
            aiVotes:
              type: array
              items:
                $ref: "#/components/schemas/AiVote"
          required: [snapshot, gates, aiVotes]

    TraderPosition:
      type: object
      properties:
        id:
          type: string
        signalId:
          type: string
        side:
          type: string
          enum: [BUY, SELL]
        entry:
          type: number
        stopLoss:
          type: number
        takeProfit:
          type: number
        sizeUnits:
          type: number
        riskAmount:
          type: number
        openedAt:
          type: string
          format: date-time
        closedAt:
          type: string
          format: date-time
          nullable: true
        exitPrice:
          type: number
          nullable: true
        exitReason:
          type: string
          nullable: true
        pnl:
          type: number
          nullable: true
        pnlR:
          type: number
          nullable: true
        status:
          type: string
          enum: [OPEN, CLOSED]
        currentPrice:
          type: number
          nullable: true
        unrealizedPnl:
          type: number
          nullable: true
      required:
        - id
        - signalId
        - side
        - entry
        - stopLoss
        - takeProfit
        - sizeUnits
        - riskAmount
        - openedAt
        - status

    CycleRunResult:
      type: object
      properties:
        ranAt:
          type: string
          format: date-time
        ok:
          type: boolean
        signalCreated:
          type: boolean
        signalId:
          type: string
          nullable: true
        signalStatus:
          type: string
          nullable: true
        rejectionReason:
          type: string
          nullable: true
        positionsClosed:
          type: integer
        gates:
          type: array
          items:
            $ref: "#/components/schemas/GateDecision"
      required: [ranAt, ok, signalCreated, positionsClosed, gates]

    ConsensusThresholds:
      type: object
      properties:
        minDeterministicAgents: { type: integer }
        minLlmAgents: { type: integer }
        minGlobalConfidence: { type: number }
        maxTrapScore: { type: number }
        minDataCompleteness: { type: number }
      required: [minDeterministicAgents, minLlmAgents, minGlobalConfidence, maxTrapScore, minDataCompleteness]

    ConsensusGuardResult:
      type: object
      properties:
        agentId: { type: string }
        passed: { type: boolean }
        reasons:
          type: array
          items: { type: string }
        penaltyScore: { type: number }
        adjustedConfidence: { type: number }
      required: [agentId, passed, reasons, penaltyScore, adjustedConfidence]

    ConsensusAgentOutput:
      type: object
      properties:
        agentId: { type: string }
        agentName: { type: string }
        vote: { type: string, enum: [BUY, SELL, NEUTRAL, ABSTAIN] }
        confidence: { type: number }
        reasoning: { type: string }
        latencyMs: { type: number }
        evidence:
          type: object
          properties:
            sources: { type: array, items: { type: string } }
            features_used: { type: array, items: { type: string } }
            timestamp: { type: string }
          required: [sources, features_used, timestamp]
        signals: { type: object }
      required: [agentId, agentName, vote, confidence, reasoning, latencyMs]

    ConsensusGuardedAgent:
      type: object
      properties:
        output:
          $ref: "#/components/schemas/ConsensusAgentOutput"
        guard:
          $ref: "#/components/schemas/ConsensusGuardResult"
      required: [output, guard]

    EntryZone:
      type: object
      description: Optimal entry zone found by the consensus engine (SL ≤ 30 pips, TP 300–500 pips)
      properties:
        direction:   { type: string, enum: [BUY, SELL] }
        entry:       { type: number, description: "Entry price" }
        stopLoss:    { type: number, description: "Stop loss price" }
        takeProfit:  { type: number, description: "Take profit price" }
        slPips:      { type: integer, description: "Stop loss distance in pips ($1 per pip for XAU/USD)" }
        tpPips:      { type: integer, description: "Take profit distance in pips" }
        riskReward:  { type: number, description: "R:R ratio (tpPips / slPips)" }
        levelType:   { type: string, description: "Key level that defines the trade (e.g. Fib_0.618, Psych_4600)" }
        confidence:  { type: number, description: "Level weight/confidence 0–1" }
        source:      { type: string, description: "Which agent computed this zone" }
      required: [direction, entry, stopLoss, takeProfit, slPips, tpPips, riskReward, levelType, confidence, source]

    ConsensusVerdict:
      type: object
      properties:
        verdict: { type: string, enum: [ALLOW, BLOCK] }
        direction: { type: string, nullable: true }
        globalConfidence: { type: number }
        trapScore: { type: number }
        dataCompleteness: { type: number }
        deterministicAgreeCount: { type: integer }
        llmAgreeCount: { type: integer }
        blockReason: { type: string, nullable: true }
        computedAt: { type: string, format: date-time }
        entryZone:
          nullable: true
          allOf:
            - $ref: "#/components/schemas/EntryZone"
        thresholds:
          $ref: "#/components/schemas/ConsensusThresholds"
        agents:
          type: array
          items:
            $ref: "#/components/schemas/ConsensusGuardedAgent"
      required:
        - verdict
        - globalConfidence
        - trapScore
        - dataCompleteness
        - deterministicAgreeCount
        - llmAgreeCount
        - computedAt
        - agents

    FrameIngestCluster:
      type: object
      properties:
        price: { type: number }
        intensity: { type: number }
        type: { type: string, enum: [buy, sell, neutral] }
      required: [price, intensity, type]

    FrameIngestRequest:
      type: object
      properties:
        clusters:
          type: array
          items:
            $ref: "#/components/schemas/FrameIngestCluster"
        labels:
          type: array
          items: { type: string }
        timestamp: { type: string }
        sourceUrl: { type: string }
      required: [clusters, labels]

    IngestFrameResult:
      type: object
      properties:
        ok: { type: boolean }
        bufferedFrames: { type: integer }
        timestamp: { type: string }
      required: [ok, bufferedFrames, timestamp]

    TraderDashboard:
      type: object
      properties:
        account:
          $ref: "#/components/schemas/TraderAccount"
        settings:
          $ref: "#/components/schemas/TraderSettings"
        snapshot:
          $ref: "#/components/schemas/TraderSnapshot"
        openPositions:
          type: array
          items:
            $ref: "#/components/schemas/TraderPosition"
        recentSignals:
          type: array
          items:
            $ref: "#/components/schemas/TraderSignal"
        lastCycleAt:
          type: string
          format: date-time
          nullable: true
        nextCycleAt:
          type: string
          format: date-time
          nullable: true
        cycleRunning:
          type: boolean
        equityCurve:
          type: array
          items:
            type: object
            properties:
              t:
                type: string
                format: date-time
              equity:
                type: number
            required: [t, equity]
      required:
        - account
        - settings
        - openPositions
        - recentSignals
        - cycleRunning
        - equityCurve
